# ─────────────────────────────────────────────────────────────────────────────
# W!ldC4rd – Python Dependencies
# Install with: pip3 install -r requirements.txt --break-system-packages
# ─────────────────────────────────────────────────────────────────────────────

# ── Core HTTP (used by wildcard.py directly) ──────────────────────────────────
requests>=2.31.0       # HTTP requests: crt.sh queries, JS secret fetching,
                       # Google dork URL encoding (requests.utils.quote)
urllib3>=2.0.0         # Underlying HTTP client; InsecureRequestWarning suppression

# ── Notes on external tool dependencies ──────────────────────────────────────
# The following tools have their own requirements installed by install_tools.sh:
#
#   xnLinkFinder  — ~/tools/xnLinkFinder/requirements.txt (auto-installed)
#   EyeWitness    — ~/tools/EyeWitness/Python3/requirements.txt (auto-installed)
#                   OR via: sudo apt install eyewitness
#
# Go-based tools (subfinder, httpx, katana, etc.) have no Python dependencies.
